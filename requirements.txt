# --- Webhook 框架 ---
functions-framework==3.8.2

# --- Google GenAI REST (RAG) ---
google-generativeai>=0.4.0

# --- 向量检索 ---
langchain==0.2.11 # <--- 尝试一个更高的 0.2.x 版本，例如 0.2.11 (通常与 0.3.x 的 langchain-core 兼容)
chromadb==0.5.1
faiss-cpu
langchain-chroma==0.2.11 # <--- 保持与 langchain 版本一致，或至少更新到与 0.2.11 兼容的最新 0.2.x 版本
langchain-community==0.2.11 # <--- 保持与 langchain 版本一致
langchain-core==0.2.11 

# --- GCS ---
google-cloud-storage>=2.14.0

# --- Vertex AI Endpoint (旧分类器备用) ---
google-cloud-aiplatform==1.71.1
vertexai==1.71.1
google-auth==2.28.1